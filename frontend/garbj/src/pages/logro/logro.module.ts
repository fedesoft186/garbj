import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LogroPage } from './logro';

@NgModule({
  declarations: [
    LogroPage,
  ],
  imports: [
    IonicPageModule.forChild(LogroPage),
  ],
})
export class LogroPageModule {}
